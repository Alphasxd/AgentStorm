apiVersion: apps/v1
kind: Deployment
metadata:
  name: agentstorm-controller
  namespace: agentstorm-system
  labels:
    app.kubernetes.io/name: agentstorm-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: agentstorm-controller
  template:
    metadata:
      labels:
        app.kubernetes.io/name: agentstorm-controller
    spec:
      serviceAccountName: agentstorm-controller
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: manager
          image: ghcr.io/alphasxd/agentstorm-controller@sha256:5d6e9785b17036bc3d47157b4d2c39038a92175bbcd6d90252054624b5062f6f
          imagePullPolicy: IfNotPresent
          args:
            - --leader-elect
          ports:
            - name: metrics
              containerPort: 8080
            - name: health
              containerPort: 8081
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: [ALL]
